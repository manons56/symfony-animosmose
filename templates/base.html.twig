<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" /><!-- Enables responsive design -->
    <title>{% block title %}{% endblock %}</title>
    <meta name="description" content="Animosmose">
    <meta name="keywords" content="animosmose pension chien chevaux chats alimentation croquettes Berric 56 Morbihan Bretagne éducation canine garde à domicile">

    {# --- FONTS & ICONS --- #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Amatic+SC:wght@400;700&family=Libre+Bodoni:ital,wght@0,400..700;1,400..700&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">

    <link rel="icon" type="image/png" href="{{ asset('build/images/logonavigateur.png')}}">

    {# --- MAIN CSS COMPILED BY ENCORE --- #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body>

{# --- Cookie banner --- #}
{% if not cookieService.isConsentAccepted() and not cookieService.isConsentRejected() %}
    <div id="cookie-banner" class="cookie-banner">
        <p>Ce site utilise des cookies pour améliorer votre expérience.
            <a href="{{ path('app_content_confidentialite') }}" target="_blank" rel="noopener noreferrer">En savoir plus</a>
        </p>
        <div class='flex-button'>
            <button id="accept">Accepter</button>
            <button id="reject">Refuser</button>
        </div>
    </div>
{% endif %}
<div id="cookie-consent-status"
     data-accepted="{{ cookieService.isConsentAccepted() ? 'true' : 'false' }}"
     data-rejected="{{ cookieService.isConsentRejected() ? 'true' : 'false' }}"
     style="display: none;"></div>


<header class="header">
    {% block header_logo %}
        <img src="{{ asset('build/images/logo-animosmose.png') }}" alt="logo animosmose"/>
    {% endblock %}
    <nav>
        <button id="hamburger" aria-label="Menu" aria-expanded="false" aria-controls="menu-list">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <ul id="menu-list">
            <li class="black-underline"><a class="li-text-black" href="{{ path('app_home') }}" aria-current="{% if current_page =='home' %}page{% endif %}">Accueil</a></li>
            <li class="black-underline"><a class="li-text-black" href="{{ path('app_breeding') }}" aria-current="{%  if current_page =='breeding' %}page{% endif %}">Élevage cocker</a></li>
            <li class="black-underline"><a class="li-text-black" href="{{ path('app_pension') }}" aria-current="{%  if current_page =='pension' %}page{% endif %}">Pension et éducation</a></li>
            <li class="black-underline"><a class="li-text-black" href="{{ path('app_stable') }}" aria-current="{%  if current_page =='stable' %}page{% endif %}">Écurie</a></li>
            <li class="black-underline"><a class="li-text-black" href="{{ path('app_contact') }}" aria-current="{%  if current_page =='contact' %}page{% endif %}">Contact</a></li>
            <li class="white-underline"><a class="li-text-white" href="{{ path('app_product_list') }}" aria-current="{%  if current_page =='product' %}page{% endif %}">Notre boutique</a></li>
        </ul>
    </nav>
</header>

{# --- PAGE CONTENT --- #}
<main>
    {% block body %}{% endblock %}
</main>

<footer class="footer">
    <div class="footer-flex">
        <!-- Left column: Logo + social media -->
        <div class="footer-col-left">
            <img src="{{ asset('build/images/logo-animosmose.png')}}" alt='logo animosmose'/>
            <div class="footer-flex-reseaux">
                {# noopener --> Prevents the opened site from manipulating your original tab --> Security
                   noreferrer --> Hides your origin URL from the external site --> Privacy #}
                {# aria-hidden="true" tells the screen reader:
               “The icon is purely decorative, it adds no meaning.
                Ignore this icon; it has no relevance for you.” #}
                <a href='https://www.instagram.com/animosmose/' target="_blank" rel="noopener noreferrer" aria-label="Instagram">
                    <i class="fa-brands fa-square-instagram fa-xl" aria-hidden="true" style="color: #000000;"></i>
                </a>
                <a href='https://www.facebook.com/animosmose56' target="_blank" rel="noopener noreferrer" aria-label="Facebook">
                    <i class="fa-brands fa-square-facebook fa-xl" aria-hidden="true" style="color: #000000;"></i>
                </a>
            </div>
            <span class="copyright">Copyright 2025 © Animosmose</span>
        </div>

        <!-- Main navigation column (shop + pages) -->
        <div class="footer-nav">
            <p><a href="{{ path('app_product_list') }}">La boutique</a></p>
            <p><a href="{{ path('app_breeding') }}">Élevage</a></p>
            <p><a href="{{ path('app_pension') }}">Pension</a></p>
            <p><a href="{{ path('app_stable') }}">Écurie</a></p>
            <p><a href="{{ path('app_contact') }}">Contact</a></p>
        </div>

        <!-- Contact column + “They talk about us” -->
        <div class="footer-nav">
            <p>5 Le Petit Bodo</p>
            <p>56230 Berric</p>
            <p>06 25 81 48 45</p>
            <p>animosmose@gmail.com</p>
            <p><a href="{{ path('app_interview') }}">On parle de nous !</a></p>
        </div>

        <!-- Column CGV, FAQ, Legal notices -->
        <div class="footer-nav">
            <p><a href='{{ path('app_content_cgv') }}'>CGV</a></p>
            {# <p><a href=''>FAQ</a></p>#}
            <p><a href='{{ path('app_content_legalnotices') }}'>Mentions légales</a></p>
            <p><a href="{{ path("app_content_confidentialite") }}">Politique de confidentialité</a></p>
        </div>
    </div>
</footer>

{# --- MAIN SCRIPTS (APP.JS) --- #}
{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>
