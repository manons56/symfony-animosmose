{% extends 'base.html.twig' %}

{% block title %}Réinitialisation du mot de passe{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('authentification') }}
{% endblock %}

{% block body %}
    <div class="example-wrapper">

        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="form-errors" role="alert">
                {{ flash_error }}
            </div>
        {% endfor %}


        <h2>Réinitialisation du mot de passe</h2>

        {{ form_start(requestForm, {
            'attr': {'aria-label': 'Formulaire de demande de réinitialisation du mot de passe'}
        })
        }}

        <div class="form-row">
            {{ form_row(requestForm.email, {
                'attr': {
                    'placeholder': 'Votre adresse email',
                    'aria-required': 'true'
                }
            }) }}
        </div>

        <div class="form-row">
            <small>
                Veuillez renseigner l'adresse email utilisée lors de votre inscription.<br>
                Un lien de réinitialisation vous sera envoyé si le compte existe.
            </small>
        </div>

        <button type="submit">Confirmer</button>

        {{ form_end(requestForm) }}

    </div>


{% endblock %}



{# Role: “Forgot password” form.
Function: Display the ResetPasswordRequestFormType so that the user can enter their email address. #}



{# User clicks on “Forgot password” → request.html.twig form with ResetPasswordRequestFormType.
They submit their email address → ResetPasswordRequest is created, a token is generated.
After submission → check_email.html.twig page to inform the user.
Email sent → email.html.twig contains the link with token.
The user clicks on the link → reset.html.twig form with ChangePasswordFormType.
New password validated → hashed and saved, token deleted. #}
